<!DOCTYPE html>
<html>
<head>
  <title>SeeN_智能网站导航</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <link href="//netdna.bootstrapcdn.com/bootstrap/3.0.0/css/bootstrap-glyphicons.css" rel="stylesheet">
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-inverse" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="container">
      <div class="navbar-header">
        <a class="navbar-brand" href="#"><%= t('.website_name')%></a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
        <ul id="login" class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li class="dropdown">
              <a data-toggle="dropdown" role="button" href="#">
                <%= current_user.username if current_user.username.present?%>
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                <li><%= link_to '编辑资料', edit_user_registration_path %></li>
                <li><%= link_to "退出", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% else %>
            <li><a>想要自定义自己的页面？请选择</a></li>
            <li><%= link_to "创建", new_user_registration_path, :class => 'navbar-link' %></li>
            <li><%= link_to "登陆", new_user_session_path %></li>
          <% end %>
        </ul>

        <div class="col-md-5">
          <%=form_tag main_index_path, method: "get", id: "nav-search", class: "nav-search navbar-form navbar-left", role: "search" do %>
            <div class="input-group add-on">
              <input type="text" class="form-control" placeholder="搜索网站或标签" name="search" id="search" size='25'>
                <div class="input-group-btn">
                  <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                </div>
              </div>
          <% end %>
        </div>
    </div>
  </nav>

  <div id="main">
    <div id="content">
    <% if flash[:notice] || flash[:notice]%>
      <div class="alert alert-warning fade in">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= yield %>
    </div>
  </div>

</body>
</html>
